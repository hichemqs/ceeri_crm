<odoo>

    <record id="action_print_task_document" model="ir.actions.server">
        <field name="name">Print Task Document</field>

        <field name="model_id" ref="project.model_project_task"/>

        <field name="state">code</field>

        <field name="binding_model_id" ref="project.model_project_task"/>
        <field name="binding_type">action</field>

        <field name="code">
action = env.ref('ceeri_crm.action_report_task_document').report_action(records)
        </field>

    </record>

</odoo>